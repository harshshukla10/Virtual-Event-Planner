<!-- EVEN MORE ATTRACTIVE NAVBAR -->
<nav class="navbar navbar-expand-lg fixed-top custom-navbar">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="#">
      <img
        src="/logo.png"
        alt="Logo"
        width="120"
        height="40"
        class="d-inline-block align-text-top me-2 logo-img"
      />
    </a>
    <button
      class="navbar-toggler custom-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 fw-semibold custom-navlist">
        <li class="nav-item">
          <a class="nav-link custom-nav-link active" aria-current="page" href="/home">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav-link" href="/hostdash">Host an Event</a>
        </li>
        <li class="nav-item">
          <a class="nav-link custom-nav-link" href="/dashboard">Plan My Event</a>
        </li>
      </ul>
      <form class="d-flex ms-auto gap-2" role="search">
        <% if (!currUser) { %>
        <button
          type="button"
          class="btn custom-login-btn px-4"
          onclick="window.location.href='/login'"
        >
          Login
        </button>
        <button
          type="button"
          class="btn custom-signup-btn px-4"
          onclick="window.location.href='/signup'"
        >
          Sign Up
        </button>
        <% } %>
        <% if (currUser) { %>
        <button
          type="button"
          class="btn custom-logout-btn px-4"
          onclick="window.location.href='/logout'"
        >
          Log out
        </button>
        <% } %>
      </form>
    </div>
  </div>
</nav>

<style>
/* Modern attractive navbar styles */
.custom-navbar {
  background: rgba(245, 245, 255, 0.95);
  box-shadow: 0 6px 24px -8px #bdbdbd33, 0 1.5px 0 #f2f2f2;
  backdrop-filter: blur(7px);
  border-bottom: 1.5px solid #e8eaf6;
}

.logo-img {
  filter: drop-shadow(0 2px 4px #2221  );
  transition: transform 0.2s;
}
.logo-img:hover {
  transform: scale(1.06) rotate(-2deg);
}

.custom-navlist {
  gap: 2rem;
}

.custom-nav-link {
  font-size: 1.09rem;
  font-weight: 600;
  color: #252525;
  letter-spacing: 0.01em;
  padding: 8px 18px !important;
  border-radius: 8px;
  position: relative;
  transition: background 0.17s, color 0.17s;
  margin-right: 2px;
  z-index: 0;
  overflow: hidden;
}
.custom-nav-link.active,
.custom-nav-link:hover {
  color: #fff !important;
  background: linear-gradient(90deg, #ED213A 0%, #93291E 100%);
  box-shadow: 0 4px 16px -8px #ed213a44;
}
.custom-nav-link::after {
  content: "";
  display: block;
  position: absolute;
  left: 24%;
  bottom: 7px;
  width: 52%;
  height: 2.5px;
  background: linear-gradient(90deg, #93291E 0%, #ED213A 100%);
  border-radius: 2px;
  transform: scaleX(0);
  transition: 0.28s;
  z-index: 1;
}
.custom-nav-link.active::after,
.custom-nav-link:hover::after {
  transform: scaleX(1);
}

.custom-login-btn {
  background: linear-gradient(90deg, #11998e 0%, #38ef7d 100%);
  color: #fff;
  border: none;
  border-radius: 7px;
  font-weight: 600;
  box-shadow: 0 2px 10px -2px #11998e24;
  transition: background 0.16s, box-shadow 0.15s;
}
.custom-login-btn:hover {
  background: linear-gradient(90deg, #38ef7d 0%, #11998e 100%);
  box-shadow: 0 4px 16px -2px #11998e33;
}

.custom-signup-btn {
  background: #fff;
  color: #1976d2;
  border: 2.2px solid #1976d2;
  border-radius: 7px;
  font-weight: 600;
  transition: background 0.15s, color 0.15s;
}
.custom-signup-btn:hover {
  background: #1976d2;
  color: #fff;
}

.custom-logout-btn {
  background: linear-gradient(90deg, #ed213a 0%, #93291e 100%);
  color: #fff !important;
  border: none;
  border-radius: 7px;
  font-weight: 600;
  box-shadow: 0 2px 10px -2px #ed213a22;
  transition: background 0.2s, box-shadow 0.16s;
}
</style>